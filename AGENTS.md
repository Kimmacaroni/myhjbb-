# AGENTS.md — 명예회장봇 작업 표준

이 문서는 저장소 전체에 적용되는 개발·검증·배포 규칙이다. 모든 작업자는 작업 전에 이 문서와 [`docs/HERMES_AGENT_HANDOFF_KO.md`](docs/HERMES_AGENT_HANDOFF_KO.md)를 읽고, 작업 이력을 [`docs/WORK_LOG.md`](docs/WORK_LOG.md)에 남긴다.

## 1. 브랜치와 worktree

- 모든 변경은 작업별 전용 브랜치와 분리된 worktree에서 수행한다.
- 시작 전에 `git status --short --branch`, `git rev-parse HEAD`, 현재 worktree 경로를 확인한다.
- 사용자의 기존 미커밋 변경은 덮어쓰기·삭제·초기화하지 않는다. `git reset --hard`, 무단 checkout/restore, 강제 push를 금지한다.
- 한 브랜치 또는 worktree를 여러 작업자가 동시에 수정하지 않는다.
- 커밋과 원격 push는 서로 다른 행위다. **원격 push는 사용자의 명시적 승인을 받은 뒤에만** 수행하며, 커밋 지시를 push 승인으로 간주하지 않는다.

## 2. 작업 기록

- 코드를 수정하기 전에 `docs/WORK_LOG.md`에 **시작 기록**을 먼저 추가한다.
- 시작 기록에는 KST 시각, 작업 목적, 작업자(구현), 검토자(QA), 브랜치, worktree, 기준 SHA, 대상 파일, 계획한 검증을 적는다.
- 완료 시 같은 항목에 **완료 기록**을 추가하고 상태, 완료 KST 시각, 실제 변경 파일, 검증 명령과 결과, 커밋 SHA, 배포·push 여부, 후속 작업을 적는다.
- `WORK_LOG`는 엄격한 append-only 기록이다. 기존 항목은 삭제·수정·재정렬·덮어쓰기하지 않고, 오류나 민감정보 정정은 새 정정 블록을 추가해 원문과 정정 사유를 추적 가능하게 남긴다. 단, 이미 기록된 비밀값·개인정보·개인 절대 경로는 노출 최소화를 위해 값 자체만 중립 표기로 치환하고 별도 보안 정정 블록에 그 사실을 남긴다.
- 미완료·실패·보류도 삭제하지 말고 원인과 필요한 사용자 입력을 새 기록으로 추가한다.
- 비밀값, 개인정보, 쿠키 내용, DB 원본과 민감한 로그는 작업 기록에 넣지 않는다.

## 3. 민감정보와 영구 데이터 보호

다음 파일·디렉터리는 소스 관리 및 일반 배포 대상에서 제외하고, 내용을 출력하거나 채팅·이슈·PR·커밋에 넣지 않는다.

- `.env`, `.env.*` (`.env.example`만 공개 가능)
- `bot.db` 및 모든 `*.db`
- `youtube-cookies.txt`, `cookies.txt`, `cookies/` 등 쿠키 파일
- `models/` 및 VPS의 TTS 모델 디렉터리
- Discord 토큰, API 키, SSH 키, 비밀번호, 개인정보

작업 전후 `git status`와 staged diff를 확인한다. 민감 파일이 추적되거나 staged되면 즉시 중단하고 값은 노출하지 않은 채 사용자에게 보고한다. VPS 반영 시 기존 파일의 소유권과 권한을 유지하며, 빈 파일이나 로컬 사본으로 덮어쓰지 않는다.

## 4. 구현과 QA 분리

- 구현자와 QA 검토자는 원칙적으로 서로 다른 사람 또는 에이전트여야 한다.
- 구현자는 변경과 자체 검사를 수행하고 근거를 `WORK_LOG`에 남긴다.
- QA는 diff, 요구사항 충족, 회귀 위험, 보안·데이터 보호, 검증 결과를 독립적으로 확인한다.
- 동일 작업자가 불가피하게 자체 검토한 경우 이를 QA 완료로 표현하지 않는다. `QA 대기`로 기록하고 배포 승인을 대신하지 않는다.
- QA 지적 수정 후 관련 검사를 다시 실행하고 결과를 갱신한다.
- 상태는 `구현`, `독립 QA`, `push`, `배포` 축으로 각각 기록한다. 구현 완료를 QA 완료로, QA 완료를 push 또는 배포 승인·완료로 간주하지 않는다.

## 5. 로컬 검증과 커밋

최소 검증은 다음과 같다.

```bash
git diff --check
python3 -m compileall -q -x '(^|/)(\.git|\.venv|venv|models)(/|$)' .
```

- 변경 범위에 맞는 테스트가 있으면 함께 실행한다.
- 커밋 전에 `git diff --cached --check`와 staged 파일 목록을 확인한다.
- 하나의 작업은 검증된 논리적 단위로 커밋한다.
- 완료 보고와 `WORK_LOG`는 한국어로 작성하며, 변경 내용·검증 결과·커밋 SHA·미완료/위험·배포/push 여부를 구분한다.

## 6. VPS systemd 배포 안전 절차

**사용자의 명시적인 배포 승인 전에는 VPS 접속, 파일 반영, 서비스 재시작/중지, 자동 배포 설정·실행을 하지 않는다.** 커밋 또는 push 지시는 배포 승인으로 간주하지 않는다. 반대로 배포 승인도 원격 push 승인으로 간주하지 않으며, 두 행위는 각각 승인을 받는다.

승인된 배포에서도 다음 순서를 지킨다.

1. 대상 브랜치·커밋과 서비스명(`honorary-bot.service`)을 재확인한다.
2. 서비스 상태와 최근 로그를 먼저 확인하고 기준 상태를 기록한다.
3. 코드와 함께 `.env`, `bot.db`, 쿠키 파일, `models/`의 존재·경로·권한을 확인하되 내용을 출력하지 않는다.
4. 코드와 영구 데이터의 타임스탬프가 있는 백업을 만들고, 복구 명령과 위치를 기록한다.
5. 배포 패키지/동기화에서 보호 대상을 명시적으로 제외한다.
6. 서비스 재시작 전에 VPS 가상환경으로 Python 문법 검사와 필요한 테스트를 통과시킨다.
7. 검사가 성공한 경우에만 systemd 서비스를 재시작한다.
8. `systemctl is-active honorary-bot`과 `journalctl -u honorary-bot -n 100 --no-pager`로 활성 상태, Cog 로드, Discord 연결·명령 동기화, 새 traceback 부재를 확인한다.
9. 이상이 있으면 즉시 이전 코드·영구 데이터로 롤백하고 같은 검증을 반복한다.
10. 배포 SHA, 백업 위치, 검사 결과, 서비스 상태와 로그 검증 결과를 `WORK_LOG`에 남긴다. 로그의 비밀값은 마스킹한다.

## 7. 보고 원칙

- 사용자 보고는 한국어를 원칙으로 한다.
- 확인한 사실과 추정을 구분하고, 실행하지 않은 테스트나 배포를 완료했다고 쓰지 않는다.
- 구현 완료, 독립 QA 완료, 원격 push 완료, 배포 완료는 각각 별도 상태로 보고한다. 승인이나 실행 근거가 없는 상태를 다음 단계 완료로 올려 쓰지 않는다.
